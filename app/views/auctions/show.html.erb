<div class="col-md-8">
  <h1><%= @auction.title %></h1>
  <p>Details: <%= @auction.details %></p>
  <p>Ends On: <%= @auction.ends_on.strftime("%b %d, %Y") %></p>
  <p>Reserve Price: <%= number_to_currency(@auction.reserve_price) %></p>

  <%= simple_form_for [@auction, @bid] do |f| %>
    <%= f.input :amount %>
    <%= f.submit "Bid", class: "btn btn-info" %>
  <% end %>
  <hr>
<br>

<h3>Previous Bids</h3>
  <% @auction.bids.each do |bid| %>
    <div class="well"><%= number_to_currency(bid.amount) %> on <%= bid.created_at.strftime("%b %d, %Y") %></div>
  <% end %>
</div>

<div class="col-md-4">
  <h3>Current Price:
  <% if @auction.bids.any? %>
    <%= number_to_currency(@auction.bids.maximum(:amount)+1) %></h3>
  <% else %>
    <p>No bids... yet!</p>
  <% end %>
</div>
